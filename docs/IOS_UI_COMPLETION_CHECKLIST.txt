# iOS 界面完善清单（XBoard 模块）

更新时间：2026-02-13

本清单按页面梳理 iOS 端界面完成度，重点覆盖：
- 已完成：主流程可用、基础加载态/空态可用
- 缺文案：硬编码文本、未本地化提示
- 缺交互：按钮可用性、输入反馈、状态同步
- 缺异常态：失败提示、边界条件兜底

## 页面清单

| 页面 | 已完成 | 缺文案 | 缺交互 | 缺异常态 | 修复状态 |
| --- | --- | --- | --- | --- | --- |
| `TicketListPage` | 列表、空态、加载态、重试齐全 | 无 | 无 | 无 | 已确认 |
| `TicketDetailPage` | 详情、消息流、回复、关闭流程可用 | 存在硬编码中文与缺失 i18n key | 回复框空内容缺反馈；发送按钮未按输入状态禁用 | 关闭失败文案兜底不规范；关闭后状态刷新不及时 | 已修复 |
| `CreateTicketPage` | 表单提交流程可用 | 多处硬编码中文提示 | 输入提示与校验文案不统一 | 校验失败提示未统一走 i18n | 已修复 |
| `NoticeBanner` | 公告轮播与详情展示可用 | 空内容/未知时间/链接错误提示硬编码 | 无 | 链接异常提示未本地化 | 已修复 |
| `NoticePopupDialog` | 弹窗公告展示与确认可用 | 标题与空内容兜底硬编码 | 无 | 链接异常提示未本地化 | 已修复 |
| `InvitePage`/佣金弹窗 | 佣金转余额、提现、错误提示流程完整 | 无明显缺口 | 无 | 无 | 已确认 |
| `PaymentGatewayPage` | 打开支付、轮询、复制、重试流程完整 | 无明显缺口 | 无 | 无 | 已确认 |

## 本轮修复项

1. `TicketDetailPage`
- 替换硬编码文案为 i18n key。
- 增加空回复提示。
- 发送按钮按输入内容动态启用/禁用。
- 关闭工单成功后立即刷新详情状态。

2. `CreateTicketPage`
- 全部输入提示/校验提示切换为 i18n 文案。
- 统一主题、描述、消息等字段文案来源。

3. `NoticeBanner` / `NoticePopupDialog`
- 统一空内容、未知时间、重要通知标题文案为 i18n。
- 链接打开失败与格式错误改为本地化异常提示。

4. 本地化补全
- 在 `arb/intl_en.arb` 与 `arb/intl_zh_CN.arb` 新增 ticket/notice 相关 key，消除页面硬编码文案。
